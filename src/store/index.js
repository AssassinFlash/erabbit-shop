import { createStore } from 'vuex'

// vue2.0 new Vuex.store({}) 创建仓库
// vue3   createStore({})
export default createStore({
  state: {
    username: 'zs'
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
